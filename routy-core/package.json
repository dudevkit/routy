{
  "name": "routy-core",
  "version": "0.1.22",
  "private": true,
  "type": "module",
  "description": "routy gateway core — lean AI proxy (re-engineering of 9Router)",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "vitest run",
    "serve": "node bin/routy.mjs serve",
    "build": "node scripts/build.mjs",
    "build:min": "node scripts/build.mjs --minify",
    "smoke": "node scripts/smoke.mjs",
    "release": "node ../scripts/release.mjs",
    "release:keygen": "node ../scripts/release-keys.mjs generate",
    "release:keys": "node ../scripts/release-keys.mjs show"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "undici": "^8.10.2"
  },
  "bin": {
    "routy": "./bin/routy.mjs",
    "re-e": "./bin/routy.mjs"
  }
}
