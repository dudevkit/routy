# Keep the build context to source only — the runtime image is built from
# routy-core/dist, and scratch state must never leak into a layer.
**/node_modules
**/dist
**/.preview
9router/
rigs/
docs/
tests/
*.log
.git
.gitignore
Dockerfile
.dockerignore
