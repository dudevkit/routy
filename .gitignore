# Upstream reference clone — read-only, not part of routy history
/9router/

# Session-local artifacts
/rigs/orca-guide.json
node_modules/
*.log
.DS_Store
/rigs/bench-data/
/rigs/golden-data/
tests/golden/.tmp-data/
/tests/golden/.tmp-data/
/rigs/ree-home/
/rigs/ree-home-r3/
/rigs/chaos-home/
# local gateway state dirs (ROUTY_HOME) used by the verification rigs
/rigs/*-home/
/rigs/dbg-home/
routy-core/node_modules/
# design-phase screenshots — local reference only, not published
routy-ui/.preview/

# release signing material — never commit a private key
*.key
release-signing*

# build output (npm run build in routy-core, npm run build in routy-ui)
/routy-core/dist/
