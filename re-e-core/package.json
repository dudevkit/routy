{
  "name": "re-e-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RE-E gateway core — lean AI proxy (re-engineering of 9Router)",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "vitest run",
    "serve": "node bin/re-e.mjs serve",
    "build": "node scripts/build.mjs",
    "build:min": "node scripts/build.mjs --minify",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "undici": "^8.10.2"
  },
  "bin": {
    "re-e": "./bin/re-e.mjs"
  }
}
